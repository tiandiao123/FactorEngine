"""
FactorEngine — real-time factor computation engine with C++ kernels.

After `pip install -e .`, the C++ extensions are available as top-level modules:
    import fe_ops       # batch array-level kernels
    import fe_runtime   # DAG push-level runtime (FactorGraph, SymbolRunner, InferenceEngine)
"""
